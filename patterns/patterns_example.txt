# Regex (pkg.go.dev/regexp, "re:" prefix)
re:password[0-9]+
re:\b(secret|token|key)\b
re:user(name)?\s*=\s*\w+

# Plain (Case sensitive, "plain:" prefix)
plain:MySuperSecret
plain:admin
plain:123456

# Plain (Case insensitive, "plain:i:" prefix)
plain:i:session_id
plain:i:apikey
plain:i:authorization

# Any symbols and encoding
plain:пароль
plain:i:доступ

# Empty strings and strings started with "#" ignored
